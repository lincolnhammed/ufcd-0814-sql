# Crie um novo script que mostre todas as tabelas já criadas na base de dados. Guarde o script com o nome: Ex_a_5_mostrarTabelas 
use efaprog004_exercicios;

select * from emprego;
select * from paises;
select * from personagem;
select * from pessoa;
select * from pessoa_personagem;